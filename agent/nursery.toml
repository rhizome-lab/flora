[project]
name = "agent"
version = "0.1.0"
description = "Autonomous task execution with LLM + moss"

[spore]
entry = "init.lua"
integrations = ["llm", "moss"]

[spore.llm]
default_provider = "anthropic"
